// use reqwest;

// pub async fn fetch_songs(title: &str) -> Result<String, reqwest::Error> {
//     let url = format!("https://api.deezer.com/search?q={}", title);
//     let response = reqwest::get(url).await?;

//     let body = response.text().await?;
//     return Ok(body);
// }
